# Create a list of integers
integer_list <- 1:5

# Create a list of month names
month_list <- c("January", "February", "March", "April", "May")

# Merge the two lists
merged_list <- list(integer_list, month_list)

# Display the merged list
print(merged_list)
