
mixed_list <- list(
  string_value = "Hello, world!",
  numeric_value = 42,
  integer_vector = c(1, 2, 3),
  character_vector = c("RAM", "ROHAN", "RAMESH"),
  logical_value = TRUE
)

print(mixed_list)
